{
  "name": "logseeker",
  "displayName": "logseeker",
  "description": "a log seeker",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.63.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:logseeker.helloWorld"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "log-sets-explorer",
          "title": "Log Sets Explorer",
          "icon": "resources/file-alt-regular.svg"
        }
      ]
    },
    "views": {
      "log-sets-explorer": [
        {
          "id": "logsets",
          "name": "Log Sets"
        }
      ]
    },
    "commands": [
      {
        "command": "logsets.addlogset",
        "title": "Add A New Log Set"
      },
      {
        
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.4",
    "@types/mocha": "^9.0.0",
    "@types/node": "14.x",
    "@types/vscode": "^1.63.0",
    "@typescript-eslint/eslint-plugin": "^5.1.0",
    "@typescript-eslint/parser": "^5.1.0",
    "@vscode/test-electron": "^1.6.2",
    "eslint": "^8.1.0",
    "glob": "^7.1.7",
    "mocha": "^9.1.3",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "nanoid": "^3.1.30"
  }
}
